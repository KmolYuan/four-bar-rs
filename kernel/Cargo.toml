[package]
name = "four-bar"
version = "0.1.0"
authors = ["KmolYuan <pyslvs@gmail.com>"]
edition = "2018"
description = "Four🍀bar is a simulator, a synthesizing tool for four-bar linkage mechanism."

[features]
default = ['synthesis']
synthesis = ['ndarray', 'metaheuristics-nature', 'efd']

[dependencies]
ndarray = { version = "0.15", optional = true }
metaheuristics-nature = { version = "0.7", optional = true }
efd = { version = "0.3", optional = true }
